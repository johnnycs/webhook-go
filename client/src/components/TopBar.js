import React from 'react';
import AppBar from 'material-ui/AppBar';

const AppBarExampleIcon = () => (
  <AppBar
    title="Webhook Golang"
    showMenuIconButton={false}
  />
);

export default AppBarExampleIcon;
